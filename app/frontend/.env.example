# Frontend example environment variables for Vite/React
# Copy this file to .env or set VITE_ prefixed vars in your environment.

# Base URL for API requests (Vite proxies /api to the backend by default)
VITE_API_BASE=/api

# If your backend runs on a different host/port during development, set absolute URL:
# VITE_API_BASE=http://127.0.0.1:8000/api
